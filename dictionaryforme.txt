###############################

abbverbs:
P0 = points
P1 = prestigePoints/prestigeMult
P2 = beyondPoints

formulae for stuff:
production for P0 = gen1.amount * gen1.mult * multGain * (gen2.amount + 1) * (gen3.amount = 1) * genMult * multP1
multGain = 1.0000005^no. of generators that have been currently bought
genMult = (gen4.amount + 1) * (gen5.amount + 1) * (gen6.amount  + 1) * (gen7.amount + 1) / 10000
multP1 += production for P1
production for P1 = (P0 * totalGens / 1000)^0.16
totalGens = gen1.amount + gen2.amount + gen3.amount + gen4.amount + gen5.amount + gen6.amount + gen7.amount 